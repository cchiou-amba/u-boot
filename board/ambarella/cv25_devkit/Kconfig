if TARGET_CV25_DEVKIT

# include/configs/<target>.h
config SYS_CONFIG_NAME
	default "cv25"

# board/<board>/* (or board/<vendor>/<board>/* if CONFIG_SYS_VENDOR is defined)
config SYS_BOARD
	default "cv25_devkit"

endif
